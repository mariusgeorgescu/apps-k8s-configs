# Namespace-ul aplicației delegation-service (model app-owned): aplicația își gestionează propriul ciclu de viață.
apiVersion: v1 # Versiunea API pentru resurse de tip v1 (core)
kind: Namespace # Tipul resursei este Namespace
metadata: # Metadate despre resursă
  name: delegation-service # Numele namespace-ului creat

