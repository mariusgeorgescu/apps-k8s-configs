# Service patch for delegation-service dev environment to add environment selector.
apiVersion: v1
kind: Service
metadata:
  name: service-delegation-service
  namespace: delegation-service
spec:
  selector:
    app: delegation-service-pod-label
    environment: dev

