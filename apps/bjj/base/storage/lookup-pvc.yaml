# PersistentVolumeClaim pentru baza de date chain lookup (citire/scriere locală).
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: lookup-pvc
  namespace: bjj
spec:
  accessModes:
  - ReadWriteOnce
  resources:
    requests:
      storage: 5Gi
