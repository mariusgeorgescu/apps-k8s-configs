# Certificat TLS pentru Grafana dev prin cert-manager și GoDaddy webhook
apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: grafana-dev-cardano-vip
  namespace: bjj
spec:
  secretName: grafana-dev-cardano-vip-tls
  issuerRef:
    name: godaddy-prod
    kind: ClusterIssuer
    group: cert-manager.io
  dnsNames:
  - grafana-dev.cardano.vip
