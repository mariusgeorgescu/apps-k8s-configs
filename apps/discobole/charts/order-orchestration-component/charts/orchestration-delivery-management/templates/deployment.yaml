apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "delivery_management.fullname" . }}
  labels:
    {{- include "delivery_management.labels" . | nindent 4 }}
    {{- include "delivery_management.shutdownLabels" . | nindent 4 }}
spec:
{{- if not .Values.autoscaling.enabled }}
  replicas: {{ .Values.replicaCount }}
{{- end }}
  selector:
    matchLabels:
      {{- include "delivery_management.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
        rollme: {{ randAlphaNum 5 | quote }}
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      labels:
        {{- include "delivery_management.selectorLabels" . | nindent 8 }}
        {{- include "delivery_management.monitoringLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      serviceAccountName: {{ include "delivery_management.serviceAccountName" . }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: orchestration-delivery-management
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          env: #modified
            - name: JDK_JAVA_OPTIONS
              value: {{ .Values.jdkJavaOptions }}
            - name: SPRING_CLOUD_KUBERNETES_ENABLED
              value: "true"
            - name: SPRING_CLOUD_KUBERNETES_CONFIG_ENABLED
              value: "true"
            - name: SPRING_APPLICATION_JSON
              valueFrom:
                secretKeyRef:
                  name: {{ .Values.od.spring_json_secret_name }}
                  key: spring-db
            {{- if .Values.keycloak.enabled }}
            - name: KEYCLOAK_URI
              value: {{ .Values.keycloak.uri }}
            - name: KEYCLOAK_CLIENT_ID
              value: {{ .Values.keycloak.clientId }}
            - name: KEYCLOAK_CLIENT_SECRET
              valueFrom:
                secretKeyRef:
                  name: comd-keycloak-client-secret
                  key: KEYCLOAK_CLIENT_SECRET
            {{- end }}
            - name: KAFKA_SERVICE_NAME
              value: {{ .Values.od.service.kafka }}
            - name: SPRING_CLOUD_KUBERNETES_CONFIG_SOURCES_0_NAME
              value: "{{ template "delivery_management.fullname" . }}"
            - name: ENV_TYPE
              value: {{ .Values.environmentType }}
            - name: SPRING_PROFILES_ACTIVE
              value: kubernetes{{- if .Values.fluentd.enabled }},fluentd{{- end }},jsonconsole
            - name: SERVER_SERVLET_CONTEXT_PATH
              value: "{{ .Values.contextPath }}"
            - name: PRODUCT_CATALOG_SERVICE
              value: {{ .Values.od.service.productCatalog }}
            - name: CPIB_SERVICE
              value: {{ .Values.od.service.cpib }}
            - name: POI_SERVICE
              value: {{ .Values.od.service.poi }}
            - name: MOCK_SERVICE
              value: {{ .Values.od.service.mock }}
            - name: USER_ROLE_RETRIEVAL
              value: {{ .Values.od.service.userRoleRetrieval }}
            {{- if .Values.fluentd.enabled }}
            - name: FLUENTD_TAG
              value: "{{ default "delivery_management" .Values.fluentd.tag }}"
            - name: FLUENTD_CONTAINER_NAME
              value: "delivery_management"
            - name: FLUENTD_HOST
              value: "{{ default "localhost" .Values.fluentd.host }}"
            - name: FLUENTD_PORT
              value: "{{ default "24224" .Values.fluentd.port }}"
            {{- end }}
            {{- if .Values.otel.enabled }}
            - name: JAVA_TOOL_OPTIONS
              value: "-javaagent:/app/opentelemetry-javaagent.jar -Dotel.instrumentation.kafka.enabled=true -Dotel.instrumentation.spring-kafka.enabled=true -Dotel.instrumentation.messaging.experimental.receive-telemetry.enabled=false"
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://{{ .Values.otel.sidecar.otlp.host }}:{{ .Values.otel.sidecar.otlp.port }}"
            - name: OTEL_METRICS_EXPORTER
              value: "none"
            - name: OTEL_TRACES_EXPORTER
              value: "otlp"
            - name: OTEL_SERVICE_NAME
              value: "{{ template "delivery_management.fullname" . }}"
            - name: OTEL_RESOURCE_ATTRIBUTES
              value: "service.name={{ include "delivery_management.fullname" . }},app=disco,env={{ default "default" .Values.monitoring.environment }},container_name=delivery_management"
            {{- end }}
          ports:
            - name: http
              containerPort: 8080 #modified
              protocol: TCP
            - name: actuator
              containerPort: 9000 #modified
              protocol: TCP
          volumeMounts:
            - name: {{ template "delivery_management.fullname" . }}-applog
              mountPath: /tmp
            - name: volume-keystore
              mountPath: /mnt/keystore
              readOnly: true
          readinessProbe:
            httpGet:
              path: {{ .Values.contextPath }}/actuator/health/readiness
              port: actuator
            initialDelaySeconds: 65
            failureThreshold: 3
            periodSeconds: 10
          livenessProbe:
            httpGet:
              path: {{ .Values.contextPath }}/actuator/health/liveness
              port: actuator
            initialDelaySeconds: 200
            failureThreshold: 1
            periodSeconds: 20
          startupProbe:
            httpGet:
              path: {{ .Values.contextPath }}/actuator/health
              port: actuator
            initialDelaySeconds: 200
            failureThreshold: 45
            periodSeconds: 2
          lifecycle:
            preStop:
              exec:
                command: ["sh", "-c", "sleep 10"]
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
      volumes:
      - name: {{ template "delivery_management.fullname" . }}-applog
        emptyDir: {}
      - name: volume-keystore
        secret:
          secretName: {{ include "delivery_management.fullname" . }}-keystore-p12
          items:
            - key: keystore-p12
              path: keystore-p12
          defaultMode: 420
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
