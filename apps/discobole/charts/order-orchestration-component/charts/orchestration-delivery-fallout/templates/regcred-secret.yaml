{{- if .Values.registryCredentials.create -}}
apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: regcred-{{ template "orchestration_delivery_fallout.fullname" . }}
  labels:
    {{- include "orchestration_delivery_fallout.labels" . | nindent 4 }}
data:
  .dockerconfigjson: >-
    {{ template "orchestration_delivery_fallout.regcredData" . }}
{{- end }}