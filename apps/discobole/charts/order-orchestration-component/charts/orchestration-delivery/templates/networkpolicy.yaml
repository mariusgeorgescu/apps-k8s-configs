apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "orchestration_delivery.fullname" . }}-network-policy
  labels:
    {{- include "orchestration_delivery.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "orchestration_delivery.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  ingress:
  - {}
    