apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "order_capture_mock_server.fullname" . }}-network-policy
  labels:
    {{- include "order_capture_mock_server.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "order_capture_mock_server.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  ingress:
  - {}
    