apiVersion: v1
kind: Secret
metadata:
  name: {{ include "catalog.fullname" . }}-mongo-secret
  labels:
    {{- include "catalog.labels" . | nindent 4 }}
type: Opaque
data:
  MONGO_DB_PWD: {{ default "root" .Values.database.rootPassword | b64enc }}