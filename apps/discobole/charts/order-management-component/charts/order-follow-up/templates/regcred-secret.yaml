{{- if .Values.registryCredentials.create -}}
apiVersion: v1
kind: Secret
type: kubernetes.io/dockerconfigjson
metadata:
  name: regcred-{{ template "order_follow_up.fullname" . }}
  labels:
    {{- include "order_follow_up.labels" . | nindent 4 }}
data:
  .dockerconfigjson: >-
    {{ template "order_follow_up.regcredData" . }}
{{- end }}