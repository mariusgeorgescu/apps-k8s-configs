apiVersion: networking.k8s.io/v1
kind: NetworkPolicy
metadata:
  name: {{ include "order_inventory.fullname" . }}-network-policy
  labels:
    {{- include "order_inventory.labels" . | nindent 4 }}
spec:
  podSelector:
    matchLabels:
      {{- include "order_inventory.selectorLabels" . | nindent 6 }}
  policyTypes:
  - Ingress
  ingress:
  - {}