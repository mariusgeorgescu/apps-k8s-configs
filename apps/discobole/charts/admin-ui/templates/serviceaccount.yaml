{{- if .Values.serviceAccount.create -}}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "adminui.serviceAccountName" . }}
  labels:
    {{- include "adminui.labels" . | nindent 4 }}
  {{- with .Values.serviceAccount.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}

---

kind: Role
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "adminui.serviceAccountName" . }}-namespace-reader
  labels:
    {{- include "adminui.labels" . | nindent 4 }}
rules:
  - apiGroups: [""]
    resources: ["configmaps", "pods", "services", "endpoints", "secrets"]
    verbs: ["get", "list", "watch"]

---

kind: RoleBinding
apiVersion: rbac.authorization.k8s.io/v1
metadata:
  name: {{ include "adminui.serviceAccountName" . }}-namespace-reader-binding
  labels:
    {{- include "adminui.labels" . | nindent 4 }}
subjects:
- kind: ServiceAccount
  name: {{ include "adminui.serviceAccountName" . }}
  apiGroup: ""
roleRef:
  kind: Role
  name: {{ include "adminui.serviceAccountName" . }}-namespace-reader
  apiGroup: ""
{{- end }}