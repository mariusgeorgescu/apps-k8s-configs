# GitRepository source for the DISCOBOLE stack Helm charts.
# NOTE: Update the URL to point to your actual discobole-stack git repository.
# If the repo is private, create a secret with SSH key or HTTPS token.
apiVersion: source.toolkit.fluxcd.io/v1
kind: GitRepository
metadata:
  name: discobole-stack
  namespace: flux-system
spec:
  interval: 10m0s
  ref:
    branch: main
  url: https://github.com/mariusgeorgescu/discobole-stack.git
  # Uncomment if repository is private:
  # secretRef:
  #   name: discobole-stack-auth
