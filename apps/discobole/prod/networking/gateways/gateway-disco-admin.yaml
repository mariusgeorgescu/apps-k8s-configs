# Gateway (Gateway API) for the Admin UI portal.
apiVersion: gateway.networking.k8s.io/v1
kind: Gateway
metadata:
  name: gateway-disco-admin
  namespace: disco
spec:
  gatewayClassName: traefik
  listeners:
  - name: http
    hostname: disco-admin.e7d.tech
    port: 80
    protocol: HTTP
    allowedRoutes:
      namespaces:
        from: All
  - name: https
    hostname: disco-admin.e7d.tech
    port: 443
    protocol: HTTPS
    tls:
      mode: Terminate
      certificateRefs:
      - kind: Secret
        name: secret-disco-admin-e7d-tech-tls
    allowedRoutes:
      namespaces:
        from: All
