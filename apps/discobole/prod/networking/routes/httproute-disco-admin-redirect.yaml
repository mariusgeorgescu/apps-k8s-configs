# HTTPRoute to redirect HTTP to HTTPS for the Admin UI.
apiVersion: gateway.networking.k8s.io/v1
kind: HTTPRoute
metadata:
  name: httproute-disco-admin-redirect
  namespace: disco
spec:
  parentRefs:
    - name: gateway-disco-admin
      namespace: disco
      sectionName: http
  hostnames:
    - disco-admin.e7d.tech
  rules:
    - filters:
        - type: RequestRedirect
          requestRedirect:
            scheme: https
            statusCode: 301
