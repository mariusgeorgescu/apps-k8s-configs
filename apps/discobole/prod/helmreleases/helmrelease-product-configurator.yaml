# HelmRelease for the Product Configurator service.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: product-configurator
  namespace: disco
spec:
  interval: 10m
  chart:
    spec:
      chart: ./product-configurator
      sourceRef:
        kind: GitRepository
        name: discobole-stack
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  dependsOn:
    - name: product-catalog
      namespace: disco
  values:
    replicaCount: 1

    imagePullSecrets:
      - name: gar-access

    ingress:
      enabled: false

    service:
      type: ClusterIP
      port: 8080

    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
