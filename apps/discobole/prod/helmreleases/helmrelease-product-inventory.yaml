# HelmRelease for the Product Inventory service.
apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: product-inventory
  namespace: disco
spec:
  interval: 10m
  chart:
    spec:
      chart: ./apps/discobole/charts/product-inventory
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
  install:
    createNamespace: false
    remediation:
      retries: 3
  upgrade:
    remediation:
      retries: 3
  values:
    replicaCount: 1

    imagePullSecrets:
      - name: gar-access

    # Disable fluentd (not configured in this environment)
    fluentd:
      enabled: false

    ingress:
      enabled: false

    service:
      type: ClusterIP
      port: 8080

    mongo:
      enabled: true
      auth:
        enabled: false
      persistence:
        enabled: true
        size: 2Gi

    resources:
      requests:
        cpu: 100m
        memory: 256Mi
      limits:
        cpu: 500m
        memory: 512Mi
